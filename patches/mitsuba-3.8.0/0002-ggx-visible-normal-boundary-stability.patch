diff --git a/include/mitsuba/render/microfacet.h b/include/mitsuba/render/microfacet.h
index c6f4ce00f..375ef0b54 100644
--- a/include/mitsuba/render/microfacet.h
+++ b/include/mitsuba/render/microfacet.h
@@ -404,6 +404,12 @@ public:
             return dr::erfinv(Vector2f(x, dr::fmsub(2.f, sample.y(), 1.f)));
         } else {
             // Choose a projection direction and re-scale the sample
+            // QMC samplers can generate exact boundary values. The GGX VNDF
+            // mapping is singular at disk-boundary configurations, so map the
+            // measure-zero boundary onto the open unit square.
+            sample = dr::maximum(
+                dr::minimum(sample, Point2f(1.f - 1e-6f)),
+                Point2f(1e-6f));
             Point2f p = warp::square_to_uniform_disk_concentric(sample);

             Float s = 0.5f * (1.f + cos_theta_i);
@@ -416,7 +422,10 @@ public:
             // Convert to slope
             Float sin_theta_i = dr::safe_sqrt(1.f - dr::square(cos_theta_i));
             Float norm = dr::rcp(dr::fmadd(sin_theta_i, y, cos_theta_i * z));
-            return Vector2f(dr::fmsub(cos_theta_i, y, sin_theta_i * z), x) * norm;
+            Vector2f slope =
+                Vector2f(dr::fmsub(cos_theta_i, y, sin_theta_i * z), x) * norm;
+            return dr::select(dr::all(dr::isfinite(slope)),
+                              slope, Vector2f(0.f));
         }
     }

diff --git a/src/render/tests/test_microfacet.py b/src/render/tests/test_microfacet.py
index ecd78313b..9d17cbdc4 100644
--- a/src/render/tests/test_microfacet.py
+++ b/src/render/tests/test_microfacet.py
@@ -285,6 +285,18 @@ def test05_sample_ggx(variants_vec_backends_once):
     assert dr.allclose(ref[1], result[1], atol=1e-4)


+def test06_visible_ggx_boundary_samples_are_finite(variant_scalar_rgb):
+    mdf = mi.MicrofacetDistribution(
+        mi.MicrofacetType.GGX, 0.000984, True)
+    wi = dr.normalize(mi.Vector3f(-0.1, 0.05, 1.0))
+
+    for sample in ([0.0, 0.0], [0.0, 0.5], [0.5, 0.0], [1.0, 1.0]):
+        normal, pdf = mdf.sample(wi, sample)
+        assert dr.all(dr.isfinite(normal))
+        assert dr.isfinite(pdf)
+        assert pdf >= 0.0
+
+
 @pytest.mark.parametrize("sample_visible", [True, False])
 @pytest.mark.parametrize("alpha", [0.125, 0.5])
 @pytest.mark.parametrize("md_type_name", ['GGX', 'Beckmann'])
